# Deploy slim: solo lo que corre en serverless (Vercel) y en el demo web.
# Para el set completo con navegador y crawler:  pip install -e ".[all]"
#   (adds playwright for the browser strategy and the LLM extras)

flask
requests
beautifulsoup4
lxml
markdownify
trafilatura
mcp>=2
